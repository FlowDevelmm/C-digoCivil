# Chave da API do Google Gemini (obrigatória para o chatbot)
GEMINI_API_KEY=
# Modelo do Gemini a utilizar
GEMINI_MODEL=gemini-2.5-flash

# Caminho para o dump SQL com os artigos (por defeito: data/chatbot.sql)
# SQL_DUMP_PATH=data/chatbot.sql
